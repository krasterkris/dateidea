services:
  - type: worker
    name: dating-telegram-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python dating-bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
